#!/bin/sh

# Select a random wallpaper.
WALLDIR="$HOME/img/walls"

IMG="`find "$WALLDIR" -type f | shuf -n 1`"

# Choose wether to scale or tile:
case "$IMG" in
	*tiled*)
		feh --bg-tile "$IMG" ;;
	*)
		feh --bg-scale "$IMG" ;;
esac
