#!/bin/sh

# This file gets sourced, so we can set some vars.

# Make QT apps blend in.
export QT_QPA_PLATFORMTHEME='gtk2' DESKTOP_SESSION=gnome

# Fix Java's... weirdness.
export _JAVA_AWT_WM_NONREPARENTING=1
